# ADR-006: Version Marketplace Behavior as Configuration

Status: accepted

## Decision

Questionnaires, theme overrides, category dimensions, routing policies, content eligibility, and feature flags are versioned config with schema validation.

## Why

New marketplaces should be launched by changing configuration and content, not shared code branches.
